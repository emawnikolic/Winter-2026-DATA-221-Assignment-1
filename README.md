Ema Nikolic - DATA 221 Assignment #1

Question #1

A controlled multiplication loop where the product multiples by the currentNumber +1 until it has 
reached the threshold value of 100.

Question #2

A nested dictionary loop from strings where the program takes word(s) and calculates its length
and whether that value is even or odd.

Question #3

A nested dictionary from strings where the program takes a pair of numbers x and y, and calculates 
the product of x to the power of y, putting it in a dictionary after. 

Question #4

This program generates random numbers, sorts them, and finds the first position where a value is 
greater than or equal to a randomly chosen threshold.

Question #5

This function determines the size of each circle, verifies that both circle radii are valid positive 
integers, and returns the percentage of the bigger circle's area that the smaller circle covers.

Question #6

This method delivers the results sorted by value after calculating the proportion of components that 
are less than or equal to each unique value in a list of numbers.

Question #7

A time conversion function where it converts seconds into hours, minutes, seconds, and the period 
(AM or PM), and puts it into the format of HH MM SS PE.

Question #8 

A function using the pandas package, using data and creating a table with four coloumns; three for 
the original data, and one with the sum of all. 
